#pragma once

#include <config.h>
#include <mode/types.h>
#include <stdint.h>

typedef unsigned long Word;
typedef signed long SWord;

typedef Word VPtr;
typedef Word PAddr;
typedef Word PPtr;
typedef Word CPtr;
typedef Word DevId;
typedef Word CpuId;
typedef uint32_t LogicalId;
typedef Word NodeId;
typedef Word Dom;

typedef uint64_t TimeStamp;